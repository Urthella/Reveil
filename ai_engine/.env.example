# Provider selection (optional). If unset, auto-picks the first of
# Claude → OpenAI → Rule for which a key is present.
# AI_PROVIDER=claude

# Claude (Anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
